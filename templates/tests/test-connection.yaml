apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-starter-envs.fullname" . }}-test-connection"
  labels:
    {{- include "helm-starter-envs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-starter-envs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
