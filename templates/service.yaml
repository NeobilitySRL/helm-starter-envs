apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-starter-envs.fullname" . }}
  labels:
    {{- include "helm-starter-envs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-starter-envs.selectorLabels" . | nindent 4 }}
